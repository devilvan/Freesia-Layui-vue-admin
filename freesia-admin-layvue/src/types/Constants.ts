export enum PROCEED_CODE {
    ADD = "add",
    UPDATE = "update"
}

export enum Flag {
    ENABLED = '1',
    DISABLED = '0'
}

export enum Operate {
    ADD= "ADD",
    EDIT = "EDIT",
    COPY = "COPY"
}
